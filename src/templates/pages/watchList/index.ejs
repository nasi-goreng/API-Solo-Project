<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Ghibli API</title>
    <style>
      table {
        float: left;
        width: 50%;
        padding: 5px;
        text-align: center;
      }
    </style>
  </head>
  <button><a href="/">Back to Main</a></button>
  <h1>My Watch List</h1>
  <form action="/watchlist/update?_method=PATCH" method="POST">
    <input type="hidden" id="userId" name="userId" value="<%= userId %>"/>
    <div class="form-field">
      <input id="filmId" name="filmId" type="number"/>
      <button type="submit">Watched!</button>
    </div>
  </form>
  <table>
    <thead>
      <th scope="col">To Watch</th>
    </thead>
    <tbody>
      <% for (const film of watchedFilms) { %> <% if (!film.watched) { %>
      <tr>
        <td><%= film.id %>. <%= film.title %></td>
      </tr>
      <% } %> <% } %>
    </tbody>
    <table>
      <thead>
        <th scope="col">Watched</th>
      </thead>
      <tbody>
        <% for (const film of watchedFilms) { %> 
          <% if (film.watched) { %>
        <tr>
          <td><%= film.id %>. <%= film.title %></td>
        </tr>
        <% } %> <% } %>
      </tbody>
    </table>
  </table>
</html>
